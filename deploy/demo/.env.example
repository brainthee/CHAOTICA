# Copy to `.env` next to docker-compose.yml on the demo host and fill in.
# `.env` is gitignored — never commit real keys.

# MaxMind GeoLite2 licence key, used by the container entrypoint to download the
# GeoIP database. Optional: leave blank to skip the GeoIP download.
MAXMIND_LICENSE_KEY=
